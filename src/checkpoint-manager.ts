/**
 * Re-export from canonical location: src/pipeline/checkpoint-manager.ts
 * @module checkpoint-manager
 */
export {
  saveCheckpoint,
  loadCheckpoint,
  shouldResume,
  buildCheckpointState,
  type CheckpointState,
} from './pipeline/checkpoint-manager.js';
